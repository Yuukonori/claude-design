PORT=5050
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your-postgres-password
DB_NAME=postgres
# Set to true when DB_HOST is a Render (or other TLS-only) Postgres endpoint.
DB_SSL=false
JWT_SECRET=set-a-long-random-secret
