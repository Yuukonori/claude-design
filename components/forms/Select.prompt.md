Styled native dropdown.

```jsx
<Select label="Layout" options={['Flex', 'Grid', 'Stack']} />
<Select options={[{value:'px',label:'Pixels'},{value:'%',label:'Percent'}]} />
```

Accepts string options or `{value,label}`. Sizes `sm`/`md`/`lg`.
