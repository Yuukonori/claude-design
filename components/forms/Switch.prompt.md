Pill toggle for instant settings (vs Checkbox for form selections).

```jsx
<Switch label="Show grid" defaultChecked />
<Switch checked={live} onChange={setLive} />
```

One of the few rounded elements in the system. On = solid white track.
