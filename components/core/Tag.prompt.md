Token / filter chip, optionally removable.

```jsx
<Tag>layout</Tag>
<Tag onRemove={() => {}}>responsive</Tag>
<Tag shape="pill">beta</Tag>
```

`shape`: `sharp` (default) | `pill`. Pass `onRemove` to show the × button.
