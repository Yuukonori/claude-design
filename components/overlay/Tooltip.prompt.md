Hover label, wraps any trigger element.

```jsx
<Tooltip label="Generate code"><IconButton><i data-lucide="zap"/></IconButton></Tooltip>
<Tooltip label="Detach" side="bottom">{trigger}</Tooltip>
```

`side`: `top` (default) | `bottom` | `left` | `right`. High-contrast white-on-dark inverted surface.
